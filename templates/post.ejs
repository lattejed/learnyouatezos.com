<%- include('header'); -%>

<h1><%= page.title %></h1>
<p class="date"><%= page.date %></p>

<%- page.content -%>

<% page.tags.forEach((tag) => { %>
  <span class="tag"><%= tag %></span>
<% }); %>

<%- include('footer'); -%>
